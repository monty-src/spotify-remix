/** @type {import('next').NextConfig} */

/**
 * An object that exports the configuration options for the app.
 * 
 * @typedef {object} Config
 * @property {boolean} reactStrictMode - A flag that indicates if React should run in Strict Mode.
 */

/**
 * The exported configuration options.
 * 
 * @type {Config}
 */
module.exports = {
  reactStrictMode: true,
}
